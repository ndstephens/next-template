import { type Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Home Page',
};

export default function HomePage() {
  return (
    <section>
      <h1>Next Template</h1>
    </section>
  );
}
