# By default Prettier ignores files in version control systems directories (".git") and `node_modules`.  Prettier will also follow rules specified in the ".gitignore" file if it exists in the same directory from which it is run.

pnpm-lock.yaml